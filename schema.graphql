type Author {
  firstName: String
  id: ID
  lastName: String
}

type Book {
  author: Author
  id: ID
  name: String
  pageCount: Int
}

type Mutation {
  createBook(authorId: ID, name: String, pageCount: Int): Book
}

type Query {
  bookById(id: ID): Book
  books: [Book]
}
